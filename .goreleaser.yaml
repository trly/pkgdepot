version: 2

project_name: pkgdepot

builds:
  - id: pkgdepot
    main: ./cmd/pkgdepot
    binary: pkgdepot
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64

archives:
  - formats:
      - tar.gz

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^chore\(deps\):'
      - '^Merge pull request'
      - '^Merge branch'
